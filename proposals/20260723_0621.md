# Auto-drafted improvement proposal

A common failure point for "TrendEngine" is poor data quality leading to irrelevant content suggestions. A small, concrete fix would be to implement a simple relevance score for each trend, filtering out items with a score below a defined threshold (e.g., `if relevance_score < 0.5: continue`). This ensures the content pipeline only processes genuinely viable trends, improving the quality of the affiliate content being generated.
